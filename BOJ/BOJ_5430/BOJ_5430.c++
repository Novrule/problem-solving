#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<int> input(int n, string arr) {
  vector<int> v;

  if (n) {
    string tmp = "";
  }

  return v;
}

int main(void) {
  int T;

  cin >> T;

  while (T--) {
    int n;
    string p, arr;
    vector<int> v;

    cin >> p >> n >> arr;

    v = input(n, arr);
  }

  return 0;
}